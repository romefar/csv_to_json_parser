const { errorLog, successLog, warningLog } = require('./logs')
const { separatorDetector } = require('./sepratorDetector')

module.exports = {
  errorLog,
  successLog,
  warningLog,
  separatorDetector
}
